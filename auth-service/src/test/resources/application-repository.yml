eureka:
  client:
    enabled: false

spring:
  data:
    mongodb:
      port: 0

hystrix:
  command:
    default:
      execution:
        isolation:
          thread:
            timeoutInMilliseconds: 1000
